import { useState } from "react";
import Form from "../Form";

const AI = () => {
	const [directions, setDirections] = useState("");

	return (
		<div>
			<Form />
		</div>
	);
};

export default AI;
